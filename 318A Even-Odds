#include <bits/stdc++.h>
using namespace std;
 
#define ll unsigned long long int
 int main()
 {
     ll n,k,x;
     cin>>n>>k;
     if(n%2!=0)
     {
         x=(n/2)+1;
     }
     else
     x=n/2;
     if(k<=x)
     cout<<2*k -1;
     else
     cout<<2*(k-x);
     return 0;
 }
